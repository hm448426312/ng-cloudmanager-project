import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';
export declare class ModalTipComponent {
    private activeModal;
    constructor(activeModal: NgbActiveModal);
    closeModal(type: any): void;
}
